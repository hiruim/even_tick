import 'package:flutter/material.dart';

class Paymant extends StatelessWidget {
  const Paymant({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Paymant'),
      ),
      body: Center(
        child: Text('Paymant'),
      ),
    );
  }
}
